# Dependencies
node_modules/
frontend/node_modules/

# Production / Build Output
dist/
frontend/dist/

# Python / Flask
__pycache__/
*.py[cod]
*$py.class
venv/
env/
ENV/
.env
.env.local
.env.*.local
backend/__pycache__/
backend/venv/

# Environment & Config Overrides
.env
.env.local

# IDE & Editor files
.vscode/
.idea/
*.swp
*.swo

# OS System Files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs & Debugging
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Built installer binaries served by the download endpoint (not versioned)
backend/artifacts/*.exe
backend/artifacts/*.zip

# The C# agent lives in its own git repository (dhina-ss/EndPoint-Management).
# Keep it out of this deploy repo so it is never embedded as a stray gitlink
# and its build output never bloats the Render clone.
/EMS/
